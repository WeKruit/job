# Async PostgreSQL connection string used by SQLAlchemy + asyncpg
DATABASE_URL="postgresql+asyncpg://postgres:postgres@localhost:5434/job_db"
# Optional: force SQLModel metadata.create_all on startup (default auto-detects sqlite only)
DATABASE_AUTO_CREATE="false"

# Embedding config (SiliconFlow + Qwen via OpenAI-compatible API)
EMBEDDING_PROVIDER="openai"
EMBEDDING_API_KEY=""
EMBEDDING_API_BASE="https://api.siliconflow.cn/v1"
EMBEDDING_MODEL="Qwen/Qwen3-Embedding-0.6B"
EMBEDDING_DIM="768"
EMBEDDING_BATCH_SIZE="32"

# LLM config for JD parsing (SiliconFlow + Qwen via OpenAI-compatible API)
LLM_PROVIDER="openai"
LLM_MODEL="Qwen/Qwen2.5-72B-Instruct"
LLM_API_KEY=""
LLM_API_BASE="https://api.siliconflow.cn/v1"

# GeoNames
GEONAMES_USERNAME=""

# Optional legacy Gemini key (leave empty if not using Gemini)
GEMINI_API_KEY=""

# Firestore (path to Firebase service account JSON key)
FIRESTORE_CREDENTIALS_FILE=""

# Safety guards (protect against accidental bulk writes to shared databases)
# READ_ONLY_MODE: blocks all POST/PATCH/DELETE API calls when "true" (default: true)
READ_ONLY_MODE="true"
# INGEST_MAX_SOURCES: hard cap on number of sources per ingest run (default: 5)
INGEST_MAX_SOURCES="5"
